<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" type="text/css" />
    <title>Book Appointment</title>
  </head>
  <body>
    <div class="info">
      <h1 style="background-image: linear-gradient(#e84141, #e89141); border-radius: 20px; padding: 5px;">Dr. N. K. Suman Skin Clinic</h1>
      <h2>MBBS, (DMCH Darbhanga), MD</h2>
      <h2 style="color: yellow;">Address: Dhobi Tola, Ward No.8, Surat Ganj, Madhubani, Bihar 847211</h3>
      <h2>Phone: 088772 65544</h3>
        <div class="timings">
          <h2>Hours:</h2>
          <h3>Tuesday: 1-5PM</h3>
          <h3>Thursday: 1-5PM</h3>
          <h3>Saturday: 1-5PM</h3>
      </div>
    </div>
    <div class="form">
      <h2>Book your appointment</h2>
      <% const day = new Date().getDay() %> 
      <form action="/book" method="POST" id="booking-form">
        <% if(day==2 || day==4 || day==6) { %>
        <fieldset>
        <% } %>
        <% if(day!==2 && day!==4 && day!==6) { %>
          <h2 style="border: 5px dashed black; padding: 10px;">Sorry, Doctor is unavailable today. Come back tomorrow.</h2>
          <fieldset disabled>
        <% } %>
          <div class="input-field">
            <label for="name">Name: </label>
            <input type="text" id="name" name="name" required />
          </div>
          <div class="input-field">
            <label for="mobile">Mobile No.: </label>
            <input type="number" id="mobile" name="mobile" required />
          </div>
          <div class="input-field">
            <label for="address">Address: </label>
            <input type="text" name="address" id="address" required />
          </div>
          
            <div id="agender">
              <div class="input-field">
              <label for="gender">Gender: </label>
              <select name="gender" id="gender" required>
                <option value="Male" selected>Male</option>
                <option value="Female">Female</option>
                <option value="Other">Other</option>
              </select>
              </div>

              <div class="input-field">
              <label for="age">Age: </label>
              <input type="number" id="age" name="age" required />
              </div>
            </div>
        </fieldset>
        <% if(day==2 || day==4 || day==6) { %>
          <button type="submit" id="book">Book Appointment</button>
        <% } %>
        <% if(day!==2 && day!==4 && day!==6) { %>
          <button type="submit" id="book" disabled>Book Appointment</button>
        <% } %>
      </form>
    </div>
  </body>
</html>
